{% extends 'base.html.twig' %}
{% block tile %}Les stage de {{ user.name }}{% endblock %}
{% block body %}
    <div class="container">
        <h1>Stages de {{ user.name }}</h1>
        <div class="row">
            {% for stage in user.workshops %}
                {% include ':workshop/partials:_workshops.html.twig' %}
            {% endfor %}
        </div>
    </div>
{% endblock %}
