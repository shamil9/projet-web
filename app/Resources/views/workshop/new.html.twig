{% extends 'base.html.twig' %}
{% block title %}Ajouter un stage{% endblock %}

{% block body %}
    <div class="container">
        <div class="row main">
            {{ form_errors(form) }}
            <div class="col-lg-8">
                <form method="post" class="form" action="{{ path('stage_new', {'slug': user.slug}) }}">
                    {% include 'partials/pro_member/partials/_workshop_new.html.twig' %}
                    {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
