{% extends 'base.html.twig' %}

{% block title %}Liste de prestataires{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
    {% for user in users %}
        <div class="col-lg-6">
            <div class="panel panel-default">
                <div class="panel-body">
                    <div class="media">
                        <div class="media-left media-top">
                            <a href="{{ path("user_profile", {'slug': user.slug}) }}">
                                <img class="media-object img--100x100" src="{{ avatars ~ user.picture }}" alt="">
                            </a>
                        </div>
                        <div class="media-body">
                            <h4 class="media-heading">
                                <a href="{{ path('user_profile', {'slug': user.slug}) }}">{{ user.name }}</a>
                            </h4>
                            <p>{{ user.description }}</p>
                            <small class="glyphicon glyphicon-map-marker" aria-hidden="true"></small> {{ user.city }}
                            <span class="pull-right"><small class="glyphicon glyphicon-earphone
" aria-hidden="true"></small> {{ user.phone }}</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    {% endfor %}
    </div>
</div>
{% endblock %}

