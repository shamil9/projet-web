{{ form_start(form, {
    'attr':{
        'class': 'form-horizontal dropzone',
        'id': 'slider-images',
        'enctype': 'multipart/form-data'
    },
    'action': path('slider_new')
}) }}
{{ form_end(form) }}
